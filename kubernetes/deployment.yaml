apiVersion: apps/v1
kind: Deployment
metadata:
  name: lamp-ai-deployment
spec:
  replicas: 1
  selector:
    matchLabels:
      app: lamp-ai
  template:
    metadata:
      labels:
        app: lamp-ai
    spec:
      containers:
        - name: lamp-ai
          image: 956463122808.dkr.ecr.ap-northeast-2.amazonaws.com/lamp-ai:v1
          ports:
            - containerPort: 8080
          env:
            - name: KAFKA_BOOTSTRAP_SERVERS
              valueFrom:
                secretKeyRef:
                  name: operation-secrets
                  key: kafka.bootstrap.server
            - name: AWS_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: operation-secrets
                  key: s3.accessKey
            - name: AWS_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: operation-secrets
                  key: s3.secretKey