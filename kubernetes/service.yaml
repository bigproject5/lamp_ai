apiVersion: v1
kind: Service
metadata:
  name: lamp-ai
  labels:
    app: lamp-ai
spec:
  selector:
    app: lamp-ai
  ports:
    - protocol: TCP
      port: 8080
      targetPort: 8080
  type: ClusterIP
