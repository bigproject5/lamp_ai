# lamp_ai
전조등/후미등 ai 

1.git clone https://... 프로젝트를 클론합니다.
2.cp .env.example .env 명령어로 설정 파일 템플릿을 복사합니다. (Windows에서는 copy .env.example .env)
3.방금 생성된 .env 파일을 열어 비어있는 AWS_ACCESS_KEY_ID와 AWS_SECRET_ACCESS_KEY에 자신의 키를 채워 넣습니다.
4.docker-compose up을 실행하면 모든 것이 정상적으로 동작합니다.
